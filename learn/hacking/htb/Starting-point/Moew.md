- Quét service/port với `nmap`
```
nmap -sV {targetip}
```
`-sV` is for version detection
- Telnet is old protocol for remote control, bảo mất kém
- Có thể **bruteforce** user password auth của telnet